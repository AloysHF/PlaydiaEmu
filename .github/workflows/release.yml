name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: playdia-emu-linux-x86_64
          - platform: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            artifact: playdia-emu-linux-aarch64
          - platform: macos-latest
            target: x86_64-apple-darwin
            artifact: playdia-emu-macos-x86_64
          - platform: macos-latest
            target: aarch64-apple-darwin
            artifact: playdia-emu-macos-aarch64
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: playdia-emu-windows-x86_64

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust artifacts
        uses: swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install Linux build dependencies
        if: startsWith(matrix.platform, 'ubuntu')
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev libx11-dev libxkbcommon-dev libudev-dev

      - name: Build standalone
        run: cargo build -p playdiaemu --release --target ${{ matrix.target }}

      - name: Build libretro core
        run: cargo build -p playdiaemu-libretro --release --target ${{ matrix.target }}

      - name: Stage libretro core
        shell: bash
        run: |
          out="libretro/${{ matrix.artifact }}"
          mkdir -p "$out"
          rel="target/${{ matrix.target }}/release"
          # Cargo names the cdylib after its lib target (playdiaemu); copy it
          # to the playdiaemu_libretro.<ext> name RetroArch expects.
          for f in "playdiaemu.dll:playdiaemu_libretro.dll" \
                   "libplaydiaemu.so:playdiaemu_libretro.so" \
                   "libplaydiaemu.dylib:playdiaemu_libretro.dylib"; do
            src="${f%%:*}"; dst="${f##*:}"
            if [ -f "$rel/$src" ]; then
              cp "$rel/$src" "$out/$dst"
              break
            fi
          done
          cp crates/playdiaemu-libretro/playdiaemu_libretro.info "$out/playdiaemu_libretro.info"
          ls -la "$out"

      - name: Package (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          cd target/${{ matrix.target }}/release
          tar czf ../../../${{ matrix.artifact }}.tar.gz playdia-emu
          cd ../../..

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          cd target/${{ matrix.target }}/release
          7z a ../../../${{ matrix.artifact }}.zip playdia-emu.exe
          cd ../../..

      - name: Package libretro (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          cd libretro
          tar czf ../${{ matrix.artifact }}-libretro.tar.gz ${{ matrix.artifact }}
          cd ..

      - name: Package libretro (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          cd libretro
          7z a ../${{ matrix.artifact }}-libretro.zip ${{ matrix.artifact }}
          cd ..

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: |
            ${{ matrix.artifact }}.*
            ${{ matrix.artifact }}-libretro.*

  build-android:
    name: Build (Android)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,i686-linux-android,x86_64-linux-android

      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r27c

      - name: Install cargo-ndk
        run: cargo install cargo-ndk --locked

      - name: Cache Rust artifacts
        uses: swatinem/rust-cache@v2
        with:
          key: release-android

      - name: Build libretro core for Android
        run: |
          cargo ndk \
            -t arm64-v8a -t armeabi-v7a -t x86 -t x86_64 \
            --platform 21 \
            build -p playdiaemu-libretro --release

      - name: Stage libretro core
        shell: bash
        run: |
          out="libretro/playdia-emu-android"
          mkdir -p "$out/arm64-v8a" "$out/armeabi-v7a" "$out/x86" "$out/x86_64"
          cp target/aarch64-linux-android/release/libplaydiaemu.so "$out/arm64-v8a/playdiaemu_libretro_android.so"
          cp target/armv7-linux-androideabi/release/libplaydiaemu.so "$out/armeabi-v7a/playdiaemu_libretro_android.so"
          cp target/i686-linux-android/release/libplaydiaemu.so "$out/x86/playdiaemu_libretro_android.so"
          cp target/x86_64-linux-android/release/libplaydiaemu.so "$out/x86_64/playdiaemu_libretro_android.so"
          cp crates/playdiaemu-libretro/playdiaemu_libretro.info "$out/"
          ls -laR "$out"

      - name: Package libretro
        shell: bash
        run: |
          cd libretro
          tar czf ../playdia-emu-android-libretro.tar.gz playdia-emu-android
          cd ..

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: playdia-emu-android
          path: playdia-emu-android-libretro.tar.gz

  build-ios:
    name: Build (iOS)
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios,x86_64-apple-ios,aarch64-apple-ios-sim

      - name: Cache Rust artifacts
        uses: swatinem/rust-cache@v2
        with:
          key: release-ios

      - name: Build libretro core (arm64 device)
        run: cargo build -p playdiaemu-libretro --release --target aarch64-apple-ios

      - name: Build libretro core (x86_64 device)
        run: cargo build -p playdiaemu-libretro --release --target x86_64-apple-ios

      - name: Build libretro core (arm64 simulator)
        run: cargo build -p playdiaemu-libretro --release --target aarch64-apple-ios-sim

      - name: Create universal binary and stage
        shell: bash
        run: |
          out="libretro/playdia-emu-ios"
          mkdir -p "$out/simulator"
          lipo -create \
            target/aarch64-apple-ios/release/libplaydiaemu.dylib \
            target/x86_64-apple-ios/release/libplaydiaemu.dylib \
            -output "$out/playdiaemu_libretro_ios.dylib"
          cp target/aarch64-apple-ios-sim/release/libplaydiaemu.dylib \
             "$out/simulator/playdiaemu_libretro_ios.dylib"
          cp crates/playdiaemu-libretro/playdiaemu_libretro.info "$out/"
          ls -laR "$out"

      - name: Package libretro
        shell: bash
        run: |
          cd libretro
          tar czf ../playdia-emu-ios-libretro.tar.gz playdia-emu-ios
          cd ..

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: playdia-emu-ios
          path: playdia-emu-ios-libretro.tar.gz

  build-webos:
    name: Build (webOS)
    runs-on: ubuntu-latest
    env:
      WEBOS_TOOLCHAIN_URL: https://github.com/openlgtv/buildroot-nc4/releases/latest/download/arm-webos-linux-gnueabi_sdk-buildroot-x86_64.tar.gz
      RUST_TARGET: armv7-unknown-linux-gnueabi

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ env.RUST_TARGET }}

      - name: Install webOS toolchain
        shell: bash
        run: |
          curl -L "$WEBOS_TOOLCHAIN_URL" -o "$RUNNER_TEMP/webos-toolchain.tar.gz"
          mkdir -p "$RUNNER_TEMP/webos-toolchain"
          tar xf "$RUNNER_TEMP/webos-toolchain.tar.gz" \
            -C "$RUNNER_TEMP/webos-toolchain" --strip-components=1
          cd "$RUNNER_TEMP/webos-toolchain"
          ./relocate-sdk.sh
          echo "$RUNNER_TEMP/webos-toolchain/bin" >> "$GITHUB_PATH"

      - name: Cache Rust artifacts
        uses: swatinem/rust-cache@v2
        with:
          key: release-webos

      - name: Build libretro core
        env:
          CARGO_TARGET_ARMV7_UNKNOWN_LINUX_GNUEABI_LINKER: arm-webos-linux-gnueabi-gcc
          CC_armv7_unknown_linux_gnueabi: arm-webos-linux-gnueabi-gcc
          AR_armv7_unknown_linux_gnueabi: arm-webos-linux-gnueabi-ar
        run: cargo build -p playdiaemu-libretro --release --target ${{ env.RUST_TARGET }}

      - name: Verify ELF binary
        shell: bash
        run: |
          arm-webos-linux-gnueabi-readelf -h target/${{ env.RUST_TARGET }}/release/libplaydiaemu.so
          arm-webos-linux-gnueabi-readelf -d target/${{ env.RUST_TARGET }}/release/libplaydiaemu.so

      - name: Stage libretro core
        shell: bash
        run: |
          out="libretro/playdia-emu-webos"
          mkdir -p "$out"
          cp target/${{ env.RUST_TARGET }}/release/libplaydiaemu.so "$out/playdiaemu_libretro.so"
          cp crates/playdiaemu-libretro/playdiaemu_libretro.info "$out/"
          ls -laR "$out"

      - name: Package libretro
        shell: bash
        run: |
          cd libretro
          tar czf ../playdia-emu-webos-libretro.tar.gz playdia-emu-webos
          cd ..

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: playdia-emu-webos
          path: playdia-emu-webos-libretro.tar.gz

  release:
    name: Create Release
    needs: [build, build-android, build-ios, build-webos]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          draft: true
          prerelease: false
          generate_release_notes: true
          name: "PlaydiaEmu ${{ github.ref_name }}"
          body: |
            PlaydiaEmu ${{ github.ref_name }}

            ## Downloads

            ### Standalone emulator
            - **Linux x86_64**: `playdia-emu-linux-x86_64.tar.gz`
            - **Linux aarch64**: `playdia-emu-linux-aarch64.tar.gz`
            - **macOS x86_64**: `playdia-emu-macos-x86_64.tar.gz`
            - **macOS aarch64**: `playdia-emu-macos-aarch64.tar.gz`
            - **Windows x86_64**: `playdia-emu-windows-x86_64.zip`

            ### libretro core (for RetroArch)
            - Files ending in `-libretro.*` contain `playdiaemu_libretro.<ext>`,
              plus `playdiaemu_libretro.info`, ready to drop into RetroArch.
            - **Android**: `playdia-emu-android-libretro.tar.gz` (arm64-v8a, armeabi-v7a, x86, x86_64)
            - **iOS**: `playdia-emu-ios-libretro.tar.gz` (universal arm64+x86_64 device + arm64 simulator)
            - **webOS**: `playdia-emu-webos-libretro.tar.gz` (armv7)
          files: artifacts/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
